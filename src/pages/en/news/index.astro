---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import NewsCard from '../../../components/NewsCard.astro';
import { getCollection } from 'astro:content';

const allNews = await getCollection('en-news');
const sortedNews = allNews.sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---

<BaseLayout
  title="News"
  description="Latest news, announcements, and events from the CARBODIGIPARD project."
  lang="en"
>
  <div class="page-hero">
    <div class="container">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/en">Home</a>
        <span class="breadcrumb__sep" aria-hidden="true">/</span>
        <span aria-current="page">News</span>
      </nav>
      <h1>News</h1>
      <p>Recent developments, announcements, and events from the project</p>
    </div>
  </div>

  <section class="section">
    <div class="container">
      {sortedNews.length === 0 ? (
        <p class="empty-state">No news available yet.</p>
      ) : (
        <div class="grid-3">
          {sortedNews.map((post) => (
            <NewsCard
              title={post.data.title}
              summary={post.data.summary}
              date={post.data.date}
              slug={post.id}
              category={post.data.category}
              image={post.data.image}
              lang="en"
            />
          ))}
        </div>
      )}
    </div>
  </section>
</BaseLayout>

<style>
  .empty-state {
    color: var(--color-text-muted);
    text-align: center;
    padding-block: 3rem;
  }
</style>
