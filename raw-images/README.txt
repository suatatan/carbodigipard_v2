Place raw (unoptimized) images here before running:

  npm run optimize-images

Supported formats: JPG, JPEG, PNG, WebP, TIFF, AVIF

The script will create 3 sizes per image and output WebP + JPEG variants to public/uploads/.
